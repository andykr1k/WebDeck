// const generateWebsites = require('../functions/generateWebsites');

// test('Creates Simple Website', () => {
//   expect(generateWebsites("FirstName", "LastName", "Email")).toBeTruthy();
// });

test('adds 1 + 2 to equal 3', () => {
    expect(1+2).toBe(3);
});